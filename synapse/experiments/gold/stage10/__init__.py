"""Stage 4, Stage 10 typed planning and worker-context package.

The package marker intentionally re-exports nothing.  Each owner is imported
from its concrete module so ownership and dependency direction stay visible.
"""
