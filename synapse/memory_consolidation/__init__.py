"""Synapse memory consolidation subsystem (memory spec v1.4, parts 1-3).

The subsystem gives a durable Synapse run a memory owned by a connected Gold
project: plans and segment markers, the single gateway of external actions,
learned habits loaded from the last complete boundary, and the court that alone
turns recorded experience into trust, births and effectiveness states.

It depends on the runtime core only through ``synapse.memory_points`` and on
Gold for persistence, identity, legitimacy and the exact-subject policy of
project runs. Neither the core nor Gold imports it; the canonical launch
composes it.
"""
